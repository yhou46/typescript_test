export function customAdd(num1: number, num2: number): number
{
    return num1 + num2;
};

export const enum TestEnum {
    Enum1 = "TestEnum1",
};

function customMinus(num: number): number
{
    return num;
}

let moduleVariable = 123;